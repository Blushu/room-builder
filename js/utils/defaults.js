// Default values for all controls
export const defaults = {
    lightPosition: 50,
    arcLength: 180,
    arcCenterX: 0,
    arcCenterY: 5,
    arcCenterZ: 0,
    arcRotation: 225,
    ambientIntensity: 0.4,
    directionalIntensity: 1.0,
    pointIntensity: 0.8,
    showArcPath: true,
    // Window settings
    windowWidth: 4,
    windowHeight: 3,
    windowPositionX: 0,
    windowPositionY: 1.5,
    windowPositionZ: 0
};

// Constants
export const LIGHT_DISTANCE = 15; // Distance from arc center
export const ARC_SEGMENTS = 50; // Number of segments for arc visualization
